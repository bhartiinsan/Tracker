#INDEXING
# it is a contiguous memory allocation ( memory space)
#character has a memory allocatiuon(identity/ index)
# integer-- left to right direction(positive indexing)
# negative indexing will do from right to left
s="python"

print(s, type(s))
print(s[-5])
# print(s[-7]) # error
print(s[0:8])
print(s[:-6])



#----------------------------------------------------------------------------------------------------
# subscript operator
#prefix[index]
s[1] # you want y
s[-5]
#print(ch, type(ch))